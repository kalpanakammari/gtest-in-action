#include<string>
#include <vector>
#include <numeric>
#include <regex>
#include<iostream>
using namespace std;
//void Add(const string &input); //int Add(const .char*&input)
void  Add(const string &input,void(*displayPtr)(int));
